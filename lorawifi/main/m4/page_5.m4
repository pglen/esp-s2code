include(mac/general.mac)
define(TITLE, LORAWIFI Drne Detector)dnl
include(mac/header.mac)
include(mac/styles.mac)
define(SUBTITLE, Product by Peter Glen)dnl
define(SUBBANNER, Main Page)
include(mac/bannerdd.mac)
define(DDSTATUS, Active)
define(DDCOLOR, `background:#80ee80')

dnl define(DDSTATUS, Offline)
dnl define(DDCOLOR, `background:#8080ee')
dnl define(DDSTATUS, Alarm)
dnl define(DDCOLOR, `background:#ee8080')

include(mac/alertdd.mac)
include(mac/live.mac)
dnl include(mac/status.mac)
include(mac/maindd.mac)
dnl include(mac/fcc.mac)
include(mac/lowmenudd.mac)
include(mac/comm.mac)
include(mac/foot.mac)
